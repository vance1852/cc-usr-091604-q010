# 球迷票券服务

这是篮球俱乐部电子票券服务的 Python 起点。`app/tickets.py` 目前只描述座位票券，转让链、扫码消费、风控冻结和审计流程需要后续实现。

## 运行

```bash
python -m unittest discover -s tests -v
```

## 目录

- `app/tickets.py`：票券基础对象
- `tests/`：基础行为测试

